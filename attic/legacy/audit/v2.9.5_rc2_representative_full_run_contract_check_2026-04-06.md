# v2.9.5 rc2 Representative Full-Run Contract Check

Date: 2026-04-06
Scope: representative rc2 `run_mode=full` re-audit for benchmark and production
Status: active release-hardening evidence

This memo re-audits representative full runs on the rc2 head.
It checks contract stability only:

1. actual `run_mode=full` execution, including Layer2 outputs
2. artifact completeness and sidecar materialization
3. replay / inventory / Cap truth-source consistency
4. role and `comparison_type` separation between benchmark and production

This memo does not compare chemistry quality, score distributions, or runtime performance between roles.

## Important scope note

The older memo [realdata_full_run_contract_check_2026-04-06.md](realdata_full_run_contract_check_2026-04-06.md)
recorded pre-rc2 runs whose output directories were named `*-full` but whose manifests resolved to
`run_mode=core+rule1+cap`. That older memo remains useful as an archived timing note only.
It is not the rc2 `run_mode=full` evidence.

## Inputs

Shared inputs:

- benchmark config: `configs/9kr6_cys328.benchmark.yaml`
- production config: `configs/9kr6_cys328.production.yaml`
- stageplan: `configs/stageplan.empty.json`
- benchmark integrated companion: `outputs/realdata_v29_local_checks/2026-04-06/integrated/benchmark.json`
- production integrated companion: `outputs/realdata_v29_local_checks/2026-04-06/integrated/production.json`
- PAT diagnostics: `outputs/realdata_v29_local_checks/2026-04-06/fixtures/pat.json`
- managed theta table: `outputs/realdata_v29_local_checks/2026-04-06/fixtures/theta_rule1_table.parquet`
- Cap fixture: `outputs/realdata_v29_local_checks/2026-04-06/fixtures/caps.parquet`

Representative runs:

1. `fACR2240 benchmark full`
2. `CYS3200 production full`

Assay fixtures:

- `outputs/realdata_v29_local_checks/2026-04-06-rc2-head/fixtures/fACR2240.contract_assays.parquet`
- `outputs/realdata_v29_local_checks/2026-04-06-rc2-head/fixtures/CYS3200.contract_assays.parquet`

These assay fixtures are contract-only synthetic tables keyed by `canonical_link_id`.
They are used to exercise mapping / falsification / Layer2 artifact completeness without turning
this release-hardening audit into a chemistry-quality claim.

## Result summary

| Run | Elapsed | run_mode | comparison_type | inventory_complete | replay | fold_map | Cap truth recon | theta | Rule3 trace |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `fACR2240-benchmark-full-contract` | `313.82s` | `full` | `same-config` | `true` | `PASS` | `true` | `true` | `exact_target` | `available` |
| `CYS3200-production-full-contract-rerun` | `4057.38s` | `full` | `cross-regime` | `true` | `PASS` | `true` | `true` | `exact_target` | `available` |

## Artifact completeness

Both representative runs materialized the actual `full` contract outputs:

- `core_compounds.parquet`
- `rule1_assessments.parquet`
- `pair_features.parquet`
- `evidence_pairs.parquet`
- `mapping_table.parquet`
- `falsification_table.parquet`
- `cap_batch_eval.json`
- `qc_report.json`
- `eval_report.json`
- `collapse_figure_spec.json`
- `run_manifest.json`
- `output_inventory.json`
- `replay_audit.json`

Both representative runs also materialized the current rc2 sidecars:

- `theta_rule1_resolution.json`
- `rule3_trace_summary.json`

## Replay / validator observations

Common contract observations across both runs:

- `output_inventory.run_mode_complete == true`
- `replay_audit.result == "PASS"`
- `replay_audit.fold_map_consistency == true`
- `replay_audit.cap_truth_source_reconciliation_consistency == true`
- `replay_audit.theta_rule1_resolution_status == "exact_target"`
- `replay_audit.stale_manifest_detected == false`

Rule3 trace remained observational, not semantic:

- benchmark ordering distribution: `none x 2240`
- production ordering distribution: `none x 3200`
- proposal handling totals: `skip_no_candidates` only

This matches the rc2 expectation that Rule3 diagnostics improve explainability but do not
flow back into verdicts or SCV reason codes.

## Role separation observations

The representative pair preserved the intended role contract:

- benchmark full stayed `comparison_type = "same-config"`
- production full stayed `comparison_type = "cross-regime"`
- both runs executed `core`, `rule1`, `cap`, and `layer2`
- both runs kept Cap truth-source reconciliation in `verified` state

No evidence in this audit suggested benchmark / production role collapse.

## Output locations

Representative monitoring logs:

- `outputs/realdata_v29_local_checks/2026-04-06-rc2-head/runs/fACR2240-benchmark-full-contract/run_monitor.json`
- `outputs/realdata_v29_local_checks/2026-04-06-rc2-head/runs/CYS3200-production-full-contract-rerun/run_monitor.json`

Representative run directories:

- `outputs/realdata_v29_local_checks/2026-04-06-rc2-head/runs/fACR2240-benchmark-full-contract`
- `outputs/realdata_v29_local_checks/2026-04-06-rc2-head/runs/CYS3200-production-full-contract-rerun`

## Conclusion

The rc2 head still satisfies the representative full-run contract on one benchmark full run
and one production full run. The audit confirms actual `run_mode=full` materialization,
artifact completeness, replay readiness, sidecar availability, and role-separated
`comparison_type` behavior.
