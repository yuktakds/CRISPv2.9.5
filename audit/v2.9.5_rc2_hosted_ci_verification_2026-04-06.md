# v2.9.5 rc2 Hosted CI Required Matrix Verification

Date: 2026-04-06
Verified branch: `rc2/cli-help-unification`
Verified commit: `cac32f3fe149075d425a65eb4fa5f2cf8a819e25`
Workflow: `.github/workflows/v29-required-matrix.yml`
Hosted run: `24021676854`
Hosted URL: `https://github.com/yuktakds/CRISPv2.9.5/actions/runs/24021676854`

## Scope

This memo rechecks that the rc2 required CI set is actually closed in hosted GitHub Actions,
not only in docs. The target here is the required-set contract:

- required job names exist and match the repo-carried docs
- hosted jobs are green on the rc2 implementation head
- replay / inventory / Cap invariant coverage is still wired into the hosted gate

This is not a substitute for local heavy runs.

## Hosted result

Workflow result:

- status: `completed`
- conclusion: `success`

Observed required jobs:

| Job name | Conclusion |
| --- | --- |
| `required / benchmark-integrated-smoke` | `success` |
| `required / production-integrated-smoke` | `success` |
| `required / ci-sized-full-fixture` | `success` |
| `required / config-guard-matrix` | `success` |
| `required / replay-inventory-crosscheck` | `success` |
| `required / cap-artifact-invariants` | `success` |
| `required / v2.9.5-matrix` | `success` |

## Required-set closure check

The hosted job names above match the standing required-set references in:

- `README.md`
- `docs/v2.9.5_rc_checklist.md`
- `audit/v2.9.5_ci_required_matrix.md`
- `.github/workflows/v29-required-matrix.yml`

No hosted evidence suggested:

- job-name drift
- missing aggregate gate wiring
- missing replay / inventory validator coverage
- sidecar-related inventory drift failures

## Same-head local supplement

The rc2 implementation head was also checked locally with:

- command: `uv run pytest -q`
- result: `160 passed in 4.89s`

This local pass includes repo-only checks that are not separate hosted required jobs, including:

- `tests/test_role_safe_cli_help.py`
- `tests/v29/test_manifest_inventory.py`
- `tests/v29/test_replay_audit.py`
- `tests/v29/test_cap_invariants.py`

This matters because the hosted required set is intentionally narrow. The CLI help snapshot stays
protected by the repo test suite even though it is not a standalone required workflow job.

## Conclusion

The hosted required matrix is closed for the rc2 implementation head.
The required-set definition in docs matches the actual hosted workflow, and the aggregate
gate is green without missing replay, inventory, or Cap invariant coverage.
