# v2.9.5 Required Matrix Dry-Run Log

Date: 2026-04-03
Status: Accepted for `rc1` entry; continue as RC maintenance telemetry

This log tracks stability observations for the proposed `v2.9.5` required CI set before
the branch-protection policy is treated as final.

## Fixed decision axes

`V-1` is intentionally narrow. Judge the required set on only these four axes:

1. false fails do not appear in the required matrix
2. runtime stays within an acceptable envelope
3. reruns converge
4. benchmark / production / full fixture keep their intended CI roles

Do not expand `V-1` into benchmark-vs-production numerical comparisons.
Production remains a guard test for `cross-regime`, not a score-comparison baseline.

## Scope under observation

The dry-run scope is the required set defined in:

- `audit/v2.9.5_ci_required_matrix.md`
- `.github/workflows/v29-required-matrix.yml`

Observed areas:

1. benchmark integrated smoke
2. production integrated smoke
3. CI-sized full fixture
4. config guard matrix
5. replay audit / inventory cross-check
6. Cap artifact invariants

## Observation policy

- a `test failure` counts toward flakiness
- a `harness error` is recorded separately when the test command itself was malformed
- current judgement is accepted for `rc1` entry; additional hosted observations remain desirable but are no longer blocking

## Preliminary local observations before workflow-equivalent command lock

These runs were taken before the exact workflow-equivalent aggregate command was fixed.
They are useful as context, but they are not the primary `V-1` evidence.

| Run | Date | Scope | Result | Wall runtime |
| --- | --- | --- | --- | --- |
| P-1 | 2026-04-03 | preliminary required-set slice | `18 passed` | `5.47s` |
| P-2 | 2026-04-03 | preliminary required-set slice | `18 passed` | `5.51s` |

Interpretation:

- no immediate flaky behavior was seen
- these runs support promotion intent, but the scope was narrower than the final workflow command

## Workflow-equivalent local dry-run observations

Command:

```powershell
uv run pytest -q `
  tests/v29/test_9kr6_benchmark_smoke.py `
  tests/v29/test_9kr6_production_smoke.py `
  tests/v29/test_cli_full_smoke.py `
  tests/v29/test_cap_assay_fixtures.py `
  tests/test_cli_config_guards.py `
  tests/test_config_taxonomy.py `
  tests/v29/test_replay_audit.py `
  tests/v29/test_manifest_inventory.py `
  tests/v29/test_report_contract_matrix.py `
  tests/v29/test_cap_invariants.py
```

| Run | Date | Result | Pytest runtime | Wall runtime | Notes |
| --- | --- | --- | --- | --- | --- |
| W-1 | 2026-04-03 | `35 passed` | `3.80s` | `5.42s` | pass |
| W-2 | 2026-04-03 | `35 passed` | `3.58s` | `5.09s` | pass |
| W-3 | 2026-04-03 | `35 passed` | `3.60s` | `5.14s` | pass |

Current aggregates from workflow-equivalent runs:

- observed failures: `0 / 3`
- observed harness errors: `0 / 3`
- average pytest runtime: `3.66s`
- average wall runtime: `5.22s`
- rerun convergence: immediate on all observed runs

## Excluded harness incident

One local attempt on 2026-04-03 failed before tests started because a PowerShell harness
used the reserved variable name `$args`, which caused `uv` to receive malformed arguments.

Classification:

- type: `harness error`
- effect on flakiness score: excluded
- corrective action: rerun with a non-reserved variable name

This incident does not count as a test failure.

## Current judgement

Current state on 2026-04-03:

- false-fail rate observed so far: `0%`
- runtime envelope: acceptable for required CI
- fixture size: small and deterministic
- promotion risk: low

What is still missing before `V-1` can be marked done:

- CI observations across multiple days
- at least one record of rerun behavior from hosted CI, not only local execution

## Hosted CI observations on the canonical repository

Canonical repository:

- `yuktakds/CRISPv2.9.5`
- branch: `audit/smoke-config-semantic-drift`
- workflow: `.github/workflows/v29-required-matrix.yml`

Observed hosted workflow runs on 2026-04-06 UTC:

| Run | Trigger | Result | Workflow duration | URL |
| --- | --- | --- | --- | --- |
| H-1 | `push` | `success` | `105s` | `https://github.com/yuktakds/CRISPv2.9.5/actions/runs/24014624643` |
| H-2 | `workflow_dispatch` | `success` | `113s` | `https://github.com/yuktakds/CRISPv2.9.5/actions/runs/24014765862` |
| H-3 | `workflow_dispatch` | `success` | `224s` | `https://github.com/yuktakds/CRISPv2.9.5/actions/runs/24014767301` |

Aggregate hosted workflow observations:

- observed hosted failures: `0 / 3`
- average hosted workflow duration: `147.33s`
- max hosted workflow duration: `224s`
- rerun convergence: both manual reruns (`H-2`, `H-3`) succeeded on attempt `1`

Hosted component-job observations across the same three runs:

| Job | Successes | Duration range |
| --- | --- | --- |
| `required / benchmark-integrated-smoke` | `3 / 3` | `36s` to `42s` |
| `required / production-integrated-smoke` | `3 / 3` | `42s` to `51s` |
| `required / ci-sized-full-fixture` | `3 / 3` | `41s` to `59s` |
| `required / config-guard-matrix` | `3 / 3` | `45s` to `68s` |
| `required / replay-inventory-crosscheck` | `3 / 3` | `35s` to `45s` |
| `required / cap-artifact-invariants` | `3 / 3` | `47s` to `127s` |
| `required / v2.9.5-matrix` | `3 / 3` | `8s` to `10s` |

Interpretation against the fixed decision axes:

1. false fails: none observed in hosted CI
2. runtime envelope: acceptable; the slowest full workflow still completed in under `4` minutes
3. rerun convergence: positive; both `workflow_dispatch` reruns succeeded without requiring second attempts
4. role stability: positive; benchmark, production, and CI-sized full fixture jobs all passed on every hosted run

Because the production job is defined by `tests/v29/test_9kr6_production_smoke.py`, its
success means the `cross-regime` guard and `same-config` override rejection remained intact
on hosted CI. The benchmark and CI-sized full jobs likewise preserved their intended role
contracts by passing their dedicated smoke suites.

## Updated judgement on 2026-04-06

Current state on 2026-04-06:

- hosted false-fail rate observed so far: `0%`
- hosted runtime envelope: acceptable
- hosted rerun convergence: positive
- hosted role stability for benchmark / production / full fixture: preserved

RC interpretation on 2026-04-06:

- the observed hosted and local evidence is accepted as sufficient for `v2.9.5-rc1`
- future hosted observations should still be appended here as maintenance telemetry
- any later semantic change should be isolated to `v3.x`, not used to reopen this dry-run gate on the `v2.9.5` line

## Next observation fields

Use the following rows for continued logging.

| Run | Date | Environment | Result | Pytest runtime | Wall runtime | Classification | Notes |
| --- | --- | --- | --- | --- | --- | --- | --- |
| TBD | TBD | local or CI | TBD | TBD | TBD | pass / fail / harness error | TBD |
| TBD | TBD | local or CI | TBD | TBD | TBD | pass / fail / harness error | TBD |
| TBD | TBD | local or CI | TBD | TBD | TBD | pass / fail / harness error | TBD |
