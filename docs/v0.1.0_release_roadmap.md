# v0.1.0 Release Roadmap

Status: active  
Date: 2026-04-16  
Parent: `v3_current_boundary.md`, `v3_rp5_release_blocking_gate_plan.md`, `docs/pr05_30run_tracking.md`  
Scope: minimum path from current position (1/30) to v0.1.0 releaseable close. Boundary is unchanged throughout. This document does not authorize any scope widening, activation, or required promotion beyond the RP-5 exact gate.

---

## Current Position (2026-04-16)

| item | state |
|---|---|
| exact-unit guard audit (Step 1) | **PASS** — 5-layer alignment confirmed clean |
| PR-05 accumulation | **1 / 30** — `exploratory / v3-release-blocking` on main |
| advance docs (Steps 3 + 4) | drafted, pending activation |
| pre-merge gate | active — `docs/pr05_pre_merge_gate.md` |
| daily tracker monitor | running — `trig_015sxDsRASCLVUmaB1h4y8dW` |

---

## Phase A — Accumulation (1/30 → 30/30)

**What drives the count:** every main-branch push that triggers `v3 Readiness Exploratory` / `exploratory / v3-release-blocking` with conclusion `success` adds +1. Failure or cancellation resets to 0.

**What to do:**
- Apply `docs/pr05_pre_merge_gate.md` to every PR before merging to main.
- Keep the six frozen files untouched (see gate doc).
- Merge only low-risk, exact-unit-non-touching PRs to main.

**Low-risk work candidates (RP-5 exact-unit non-touching):**

| category | description | files affected | risk |
|---|---|---|---|
| docs | `docs/README.md` index — add new pending-close and tracker docs | `docs/README.md` | very low |
| docs | Non-authoritative background memos, usage notes | new files in `docs/` | very low |
| test | `migration_scope.py` constant/lookup unit tests | new `tests/v3/test_migration_scope.py` | low |
| test | `shadow_stability.py` edge-case unit tests | new `tests/v3/test_shadow_stability_edge_cases.py` | low |
| test | `layer0_authority.py` payload builder unit tests | new `tests/v3/test_layer0_authority_payloads.py` | low |

**What NOT to do during accumulation:**
- No changes to frozen files (see pre-merge gate).
- No changes to `comparator_scope`, `comparable_channels`, `v3_shadow_verdict`, verdict rate activation, Cap/Rule3B scope.
- No workflow promotion, required matrix mutation, or CI separation semantics change.

---

## Phase B — PR-05 Close (Step 3)

**Trigger:** `docs/pr05_30run_tracking.md` reaches `Current count: 30 / 30`.

**Branch:** `docs/v0.1.0-rp5-operational-close`  
**Commit:** `docs(v3): record RP-5 exact-unit operational close for v0.1.0`

**Actions (3 edits only):**
1. `docs/pr05_30run_tracking.md` — already at 30/30 (no additional edit needed beyond the tracker)
2. `docs/v3_rp5_operational_close.md` — change `Status: pending` → `Status: operationally closed`; fill in `Date:`
3. No scope, visibility, or authority change

**Does not change:** `comparator_scope`, `comparable_channels`, exploratory labeling, rc2 primary / v3 secondary roles, required matrix, authority layering.

---

## Phase C — v0.1.0 Releaseable Close (Step 4)

**Trigger:** Phase B merged.

**Branch:** `docs/v0.1.0-releaseable-close`  
**Commit:** `docs(release): close v0.1.0 releaseability at current partial scope`

**Actions (1 edit):**
1. `docs/v0.1.0_releaseable_close.md` — change `Status: pending` → `Status: closed`; fill in `Date:`

**Three-point check:**
1. Current boundary unchanged ✓
2. RP-5 exact gate operationally closed ✓ (from Phase B)
3. Hosted workflow remains exploratory, rc2 primary / v3 secondary unchanged ✓

---

## Phase D — Procedural Freeze / Tag (Step 5)

**Trigger:** Phase C merged.

**Actions:**
- Tag `v0.1.0` at the Phase C merge commit
- Release note: current boundary unchanged, exact RP-5 gate closed, exploratory CI remains exploratory, rc2 primary
- No new semantics, no operator activation, no stronger public claim

**Invariant:** the tag must not assert boundary widening, shadow verdict activation, or required promotion beyond the RP-5 exact gate.

---

## Milestone Summary

```
[NOW] 1/30 ──── Phase A (low-risk PRs + accumulation) ────► 30/30
                                                              │
                                                         Phase B: RP-5 close
                                                              │
                                                         Phase C: v0.1.0 releaseable
                                                              │
                                                         Phase D: tag v0.1.0
```

---

*End of document*
