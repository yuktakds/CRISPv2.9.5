# v0.1.0 Releaseable Close

Status: pending — awaiting RP-5 operational close  
Date: (update when RP-5 operational close is confirmed)  
Parent: `v3_current_boundary.md`, `v3_rp5_release_blocking_gate_plan.md`, `v3_rp5_operational_close.md`, `docs/pr05_30run_tracking.md`  
Scope: record that v0.1.0 is releaseable at the current partial scope. No new semantics, no boundary change.

---

## Releaseable Close Statement

v0.1.0 is releaseable.

This close is exact: v0.1.0 is releaseable **at the current partial scope**. The three conditions below are satisfied:

1. **Current boundary unchanged** — `comparator_scope = path_and_catalytic_partial`, `comparable_channels = ["path", "catalytic"]`, all operator surface guards and canonical invariants as defined in `v3_current_boundary.md` remain in effect. No boundary widening occurred during the RP-3 / RP-4 / RP-5 landing sequence or the PR-05 accumulation period.

2. **RP-5 exact gate operationally closed** — the release-blocking audit gate for the frozen keep-path RC evidence family has completed operational close as recorded in `v3_rp5_operational_close.md`. The gate is evidenced; the accepted baseline `rp5_release_blocking_keep_path_rc_baseline_v1` is satisfied.

3. **Hosted workflow remains exploratory, rc2 primary / v3 secondary unchanged** — `v3 Keep-Path RC Exploratory` and `v3 Readiness Exploratory` remain exploratory. `v2.9.5 Required Matrix` remains the frozen rc2 required-matrix definition. rc2 is primary; v3 is secondary. This has not changed.

---

## What This Release Does Not Assert

- Full migration completion
- `v3_shadow_verdict` activation
- Numeric `verdict_match_rate` / `verdict_mismatch_rate` on operator surface
- Cap comparable participation
- Rule3B comparable participation
- Any stronger public claim beyond `path_and_catalytic_partial`

This close records "releaseable at current partial scope." It is not a claim that migration is complete or that the boundary is maximal.

---

## Out-of-Scope Items (Untouched)

All items listed under "Still Out of Scope" in `v3_rp5_release_blocking_gate_plan.md` remain out of scope at this close. No item in that list has been addressed by v0.1.0 or moved toward inclusion.

---

*End of document*
